# 맛집 리뷰 서비스 — DESIGN.md

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-08-14 |
| 연관 문서 | PRD.md v1.2 |
| 상태 | Draft |

---

## 0. 디자인 원칙

> **화면의 유일한 색은 사진이다.**

PRD가 이 디자인에 걸어둔 조건은 하나로 요약된다. 직접 촬영한 40장이 "진짜 다녀왔다"를 증명하는 유일한 장치이고(PRD 10.5), 그 외의 모든 요소는 그것을 방해하지 않아야 한다.

세 가지 원칙으로 압축한다.

1. **사진이 주인공이고 UI는 액자다.** 액자가 눈에 띄면 그림이 안 보인다.
2. **위계는 색이 아니라 명암과 크기로 만든다.** 액센트 유채색을 쓰지 않기로 했으므로, 색이 하던 일을 대비와 타이포가 대신한다.
3. **구조는 커머스, 온도는 서체와 배경이 만든다.** 정연한 그리드 위에 크림과 명조를 올린다. 규율과 온기는 서로 다른 층에서 각자 일한다.

---

## 1. 무드

### 1.1 방향: 크림 위의 정연한 그리드

동네 밥집의 편안함을 **정돈된 그리드**로 전달한다. 아날로그한 손맛이나 노포 감성을 시각적으로 흉내 내지 않는다. 온기는 색과 서체에서 나오고, 신뢰는 정렬과 여백에서 나온다.

| 지향 | 지양 |
|---|---|
| 일정한 카드 규격, 정확한 정렬 | 손글씨 폰트, 종이 질감, 기울어진 배치 |
| 크림 배경이 만드는 부드러운 바탕 | 순백 배경의 차가움 |
| 사진이 화면에서 가장 밝고 선명한 요소 | UI가 사진과 채도로 경쟁하는 상태 |
| 조용한 안내 문구 | 느낌표, 강조 뱃지, 판촉 문구 |

### 1.2 네 개의 선택이 만든 긴장과 해소

인터뷰 답변은 서로 다른 방향을 가리켰다. 이 문서는 각각에 **다른 층위**를 배정해 충돌을 해소한다.

| 선택 | 담당하는 층 | 구체적 적용 |
|---|---|---|
| 따뜻한 크림·우드 | **배경** | `--bg`, `--surface` 의 색온도 |
| 액센트 없음 (브라운·차콜) | **UI 요소** | 버튼·칩·텍스트 전부 무채색 계열 |
| 명조 헤드 | **온기** | 헤드라인 3곳에서만 |
| 미니멀 커머스 | **구조** | 그리드, 카드 규격, 정렬 규칙 |

각 층은 독립적이다. 구조를 커머스로 가져가도 온기가 죽지 않고, 온기를 서체로 만들어도 정렬이 흐트러지지 않는다.

### 1.3 무드의 리스크 두 가지

> **리스크 A — 크림과 음식 사진의 색이 겹친다.**
> 한식 사진은 갈색·베이지·주황이 지배적이다. 배경까지 같은 계열이면 사진이 배경에 스며들어 경계가 사라진다.
>
> **대응**: `--surface`(카드 배경)를 사진보다 **명확히 밝고 채도가 낮은** 값으로 고정한다. 사진은 화면에서 가장 채도가 높은 요소여야 한다. 카드에 `--line` 보더를 둘러 경계를 물리적으로 만든다.

> **리스크 B — 커머스 그리드가 밥집을 상품으로 만든다.**
> 카드 그리드는 자연히 "매물"의 인상을 준다. 여기에 별점·리뷰 수·뱃지가 붙으면 "광고 없는 진짜 리뷰"라는 약속과 정면으로 어긋난다.
>
> **대응**: 7장 금지사항으로 강제한다. 카드에 정량 지표를 일절 넣지 않는다. PRD 10.5의 숫자 미표시 규칙이 여기서 두 번째 근거를 얻는다.

> **리스크 C — 크림 + 무채색 + 명조는 한정식집으로 미끄러진다.**
> 이 세 조합은 격식 있는 전통 브랜딩의 관용구다. 우리가 원한 것은 동네 밥집의 가벼움이다.
>
> **대응**: 명조를 **굵은 웨이트로만** 쓰고(3.2), 여백을 과하게 두지 않으며, 세로쓰기·전통 문양·먹 번짐 같은 장치를 일절 쓰지 않는다.

---

## 2. 컬러

### 2.1 팔레트: 웜 크림 + 딥 브라운 (유채색 액센트 없음)

**UI에 유채색을 쓰지 않는다.** 버튼·칩·배지·강조가 전부 브라운 계열의 명도 변주만으로 구분된다. 색은 사진이 전부 가져간다.

#### 토큰

| 토큰 | HEX | 용도 |
|---|---|---|
| `--bg` | `#F7F3EC` | 페이지 배경 (웜 크림) |
| `--surface` | `#FFFDF8` | 카드·모달 배경. bg보다 밝아 카드가 떠 보인다 |
| `--ink` | `#2B2320` | 본문·제목 기본 텍스트, **주 버튼 배경** |
| `--ink-muted` | `#635850` | 보조 텍스트, 카테고리, 거리, 캡션 |
| `--line` | `#E5DDD1` | 보더, 구분선, 비선택 칩 테두리 |
| `--on-ink` | `#F7F3EC` | 딥 브라운 위에 올라가는 텍스트 |
| `--dim` | `rgba(43, 35, 32, 0.45)` | 모달·시트 딤 배경 |

#### 명도 대비 검증 (WCAG)

| 조합 | 대비 | 판정 |
|---|---|---|
| `--ink` on `--bg` | 13.9:1 | AAA |
| `--ink` on `--surface` | 15.1:1 | AAA |
| `--ink-muted` on `--bg` | 6.2:1 | AA |
| `--ink-muted` on `--surface` | 6.8:1 | AA |
| `--on-ink` on `--ink` | 13.9:1 | AAA |

> **주 버튼의 대비는 13.9:1이다.** 오렌지나 레드 액센트 버튼(보통 3~4:1)보다 훨씬 강하다. 액센트를 포기해서 버튼이 약해지는 것이 아니라, 오히려 강해진다.

> `--ink-muted` 를 이보다 밝게 조정하지 말 것. 한 줄 메모와 거리 정보가 이 색을 쓰며, PRD 기준 방문자는 **길 위에서 밝은 햇빛 아래** 이 텍스트를 읽는다.

### 2.2 색 사용 규칙

- **UI 요소에 유채색을 쓰지 않는다.** 버튼, 칩, 배지, 링크, 아이콘 전부 `--ink` / `--ink-muted` / `--line` 안에서 해결한다.
- **선택 상태는 채움 반전으로 표현한다.** 비선택 칩은 투명 배경 + `--line` 보더, 선택 칩은 `--ink` 채움 + `--on-ink` 텍스트. 색이 아니라 명암이 뒤집힌다.
- **사진 위에는 텍스트를 얹지 않는다.** 얹어야 한다면 그라데이션 오버레이가 필요해지고, 그 순간 사진이 어두워진다. 텍스트는 사진 아래 영역에 둔다.
- 화면에서 **가장 채도가 높은 것은 항상 사진**이어야 한다. 이 조건이 깨지면 UI가 과한 것이다.

### 2.3 유채색이 필요해지는 순간

액센트를 쓰지 않기로 한 것은 **미학적 취향이 아니라 사진 보호 장치**다. 따라서 사진과 경쟁하지 않는 자리에서는 예외를 검토할 수 있다.

| 상황 | 판단 |
|---|---|
| 폼 에러 (이메일 형식 오류 등) | **허용.** 접근성상 무채색만으로는 오류 전달이 어렵다. 채도를 낮춘 벽돌색 1종만 |
| 성공/완료 피드백 | 무채색 + 아이콘으로 우선 해결. 색 도입 전 재검토 |
| 카테고리 구분 | **불가.** 8~10개 카테고리에 색을 부여하면 화면이 무지개가 되고 사진이 죽는다 |
| 상황 태그 (데이트·혼밥) | **불가.** 텍스트 라벨로 충분하다 |

---

## 3. 타이포그래피

### 3.1 방향: 명조 헤드 + 산세리프 본문

색을 뺀 자리를 서체가 메운다. 헤드에 명조를 쓰면 "오늘 뭐 먹지?"가 **앱이 묻는 말이 아니라 사람이 묻는 말**로 읽힌다. 본문은 산세리프로 받아 현대적 균형을 맞춘다.

### 3.2 폰트

| 용도 | 폰트 | 웨이트 | 비고 |
|---|---|---|---|
| 헤드 (명조) | **Noto Serif KR (본명조)** | 600 | 웨이트가 7종이라 굵은 명조를 확보할 수 있다 |
| 본문 · UI | **Pretendard** | 400 / 600 | 칩·버튼·본문·캡션 전부 |
| 숫자 | Pretendard + `tabular-nums` | 400 | 거리·날짜의 폭 흔들림 방지 |

**대안 검토**: 고운바탕은 부드럽고 온기가 있어 후보였으나 **웨이트가 400 하나뿐**이다. 1.3의 리스크 C(한정식집으로 미끄러짐)를 막으려면 굵은 명조가 필요하므로 본명조를 우선한다. 실제 렌더링 비교 후 확정한다.

### 3.3 명조 사용 범위 — 세 곳으로 제한

> **명조는 28px 이상에서만 쓴다.** 획이 가늘어 작은 크기·저해상도·모바일에서 흐려진다. 배고픈 사람이 길 위에서 읽는 화면임을 기억한다.

| 허용 | 금지 |
|---|---|
| S1 히어로 헤드 `오늘 뭐 먹지?` | 본문, 서브 카피 |
| S2 질문 문구 `어제 뭐 드셨어요?` | 버튼 라벨, 칩 |
| S4 CTA 헤드 `당신의 한 끼가…` | 가게 이름, 한 줄 메모 |
| — | 캡션, 거리, 날짜, 안내 문구 |

### 3.4 타입 스케일

| 토큰 | Desktop | Mobile | 서체 | 굵기 | 자간 | 용도 |
|---|---|---|---|---|---|---|
| `display` | 56px | 34px | 명조 | 600 | -0.02em | S1 헤드 |
| `h1` | 36px | 28px | 명조 | 600 | -0.02em | S2 질문, S4 CTA 헤드 |
| `h2` | 22px | 20px | 산세리프 | 600 | -0.01em | 섹션 제목 |
| `card-title` | 18px | 17px | 산세리프 | 600 | -0.01em | 가게 이름 |
| `body` | 16px | 15px | 산세리프 | 400 | 0 | 서브 카피, 안내 |
| `note` | 14px | 14px | 산세리프 | 400 | 0 | 한 줄 메모 |
| `caption` | 13px | 13px | 산세리프 | 400 | 0 | 거리, 방문일, 작성자 |
| `label` | 12px | 12px | 산세리프 | 600 | 0.06em | 칩, 카테고리 |

**규칙**
- 굵기는 **400과 600 두 가지만** 쓴다.
- 본문 행간 `1.65`, 명조 헤드 행간 `1.35` (명조는 산세리프보다 행간을 넉넉히 준다).
- S1 서브 카피는 **2행 고정** 줄바꿈 (PRD S1 확정 사항).
- 한 줄 최대 길이 **40자 이내** (국문 기준).

---

## 4. 레이아웃

| 항목 | 값 |
|---|---|
| 최대 콘텐츠 폭 | 1120px |
| 좌우 여백 | Desktop 64px / Tablet 40px / Mobile 20px |
| 그리드 간격 | 20px |
| 카드 열 수 | Desktop 3열 / Tablet 2열 / Mobile 1열 |
| 간격 단위 | 4px 배수 (4·8·12·16·20·24·32·48·64·96) |
| 모서리 반경 | 카드 `10px`, 버튼 `8px`, 칩 `999px`(pill), 이미지 `8px` |
| 보더 | `1px solid var(--line)` |

**결과 카드는 3~5개다**(PRD S3). 데스크톱 3열이면 3+2로 떨어지고, 마지막 행이 비어도 어색하지 않다. 이 화면은 목록이 아니라 **좁혀진 후보**이므로 빈칸이 오히려 "적게 골랐다"는 메시지가 된다.

**모바일 우선.** 320px에서 카드 1열, 사진 가로 꽉 참, 좌우 여백 20px를 기준으로 설계하고 데스크톱으로 확장한다.

---

## 5. 인터랙션 · 모션

PRD F3이 스크롤·호버 애니메이션을 v1 필수로 지정했다. 다만 **쓰는 곳과 쓰지 않는 곳을 명확히 가른다.**

| 위치 | 사용 | 사양 |
|---|---|---|
| 섹션 진입 (스크롤) | ✅ | 페이드 + 12px 상승, `500ms`, `cubic-bezier(0.2, 0, 0, 1)`. 한 번만 실행 |
| 카드 호버 | ✅ | 보더 `--line`→`--ink`, 사진 `scale(1.02)`, `200ms` |
| 칩 선택 | ✅ | 배경·텍스트 색 전환 `150ms`. 크기 변화 없음 |
| 버튼 호버 | ✅ | 배경 명도 약간 하강, `150ms` |
| **결과 지도 영역** | ❌ **금지** | PRD 9장. 정보를 읽고 결정하는 화면이므로 움직이지 않는다 |
| 로딩 shimmer | ❌ | 정적 플레이스홀더를 쓴다 |

### 5.1 스크롤 애니메이션의 조건

- **한 번만 실행한다.** 위로 다시 스크롤했을 때 재생되면 산만하고, 되돌아온 사용자가 방금 읽던 위치를 잃는다.
- **결과 도달 이후 섹션에는 쓰지 않는다.** 답을 기다리는 사람에게 등장 연출은 지연으로 느껴진다.
- 지연(stagger)은 **최대 3개까지**, 항목당 60ms. 카드 5개가 순차로 뜨면 마지막 카드까지 0.8초가 걸린다.

### 5.2 접근성

- 모든 인터랙티브 요소에 포커스 링: `2px solid var(--ink)`, `offset 2px`
- `outline: none` 으로 포커스 링을 제거하지 않는다
- 칩은 `role="button"` 또는 실제 `<button>`. `Tab`·`Space`·`Enter` 로 조작 가능
- 지도 결과는 **반드시 텍스트 리스트와 병행**한다. 지도만으로는 스크린리더 접근이 불가능하다
- `prefers-reduced-motion: reduce` 시 스크롤 애니메이션과 `scale` 전환을 모두 비활성화한다

---

## 6. 컴포넌트

### 6.1 히어로 (S1)

```
        오늘 뭐 먹지?              ← display / 명조 600

    진짜 다녀온 사람들의 리뷰로       ← body / 산세리프 400
    실패 없는 한 끼를 찾아보세요.      ← 2행 고정

     ┌──────────────────┐
     │  10초 만에 정하기   │          ← --ink 채움
     └──────────────────┘
```

- 헤드·서브·버튼이 **스크롤 없이 모두 보인다.**
- 배경은 `--bg` 단색. 이미지나 그라데이션을 깔지 않는다. 사진은 결과 화면에서 처음 등장해야 임팩트가 있다.
- 버튼 위 여백을 `--space-8` 이상 확보해 버튼이 고립되어 보이게 한다. 고립이 곧 강조다.

### 6.2 카테고리 칩 (S2)

| 상태 | 배경 | 텍스트 | 보더 |
|---|---|---|---|
| 비선택 | `transparent` | `--ink` | `1px solid var(--line)` |
| 호버 | `transparent` | `--ink` | `1px solid var(--ink-muted)` |
| **선택** | `--ink` | `--on-ink` | `1px solid var(--ink)` |

- 형태는 pill (`radius: 999px`). 사각형보다 "고르는 것"으로 읽힌다.
- 최소 터치 영역 **44×44px**. 칩 높이 44px, 좌우 여백 16px.
- 8~10개가 모바일에서 2~3행으로 자연스럽게 흐르게 한다. 가로 스크롤을 쓰지 않는다 — 가려진 선택지는 없는 선택지다.
- `기억 안 나요` 칩은 **다른 칩과 시각적으로 구분한다.** 한 칸 띄우고 `--ink-muted` 텍스트로.

### 6.3 가게 카드 (S3)

```
┌─────────────────────────┐
│                         │
│     [ 실사진 4:3 ]        │
│                         │
├─────────────────────────┤
│ 가게 이름                 │  card-title / --ink
│ 한식 · 도보 4분           │  caption / --ink-muted
│ 한 줄 메모 텍스트          │  note / --ink-muted
│ ─────────────────────   │  --line
│ JJIn · 2026.07.21       │  caption / --ink-muted
└─────────────────────────┘
```

| 요소 | 사양 |
|---|---|
| 배경 | `--surface` |
| 보더 | `1px solid var(--line)` |
| 반경 | `10px` |
| 그림자 | **없음** (7장) |
| 사진 비율 | **4:3 고정.** 이후 변경하지 않는다 |
| 내부 여백 | 16px |

**하단의 작성자·방문일이 이 카드의 핵심이다.** PRD 10.5의 요구사항이며, 이 두 줄이 없으면 카드는 익명 데이터베이스가 되고 "진짜 다녀온 사람들"이라는 카피가 거짓이 된다.

### 6.4 버튼

| 종류 | 배경 | 텍스트 | 보더 |
|---|---|---|---|
| 주 버튼 | `--ink` | `--on-ink` | 없음 |
| 보조 버튼 | `transparent` | `--ink` | `1px solid var(--line)` |

- 최소 높이 **48px**, 모바일에서는 가로 꽉 참.
- 한 화면에 주 버튼은 **하나만** 둔다.

### 6.5 제외 안내 배너 (S3 상단)

```
┌───────────────────────────────────────┐
│  어제 드신 한식은 빼고 골랐어요.          │
└───────────────────────────────────────┘
```

- 배경 `--surface`, 좌측에 `3px solid var(--ink)` 세로선, 텍스트 `--ink`
- **이 배너가 서비스의 정체성을 전달하는 유일한 문장이다.** 결과만 보여주면 평범한 추천으로 읽힌다. 눈에 띄되 결과 카드보다 앞서지 않는 크기로 둔다.

---

## 7. 금지사항

> 아래는 권고가 아니라 규칙이다. 판단이 흔들릴 때 이 목록이 기준이 된다.

### 7.1 ❌ 정량 지표 표시 금지

- **별점, 리뷰 수, 방문자 수, 저장 수, 순위를 화면에 표시하지 않는다.**
- 통계를 집계해 보여주는 UI를 만들지 않는다.

> **이유**: PRD 10.5. v1의 리뷰는 운영자 1인이 쓴 40건이다. 숫자를 적는 순간 규모가 드러나 "진짜 다녀온 사람들"이라는 카피가 거짓이 된다. 동시에 이 서비스는 "별점 4.8인데 왜 맛이 없지"라는 문제의식에서 출발했다. 별점을 다시 붙이면 비판했던 자리로 돌아간다.

### 7.2 ❌ 스톡 이미지 · 홍보 사진 금지

- 가게 사진은 **직접 촬영한 것만** 쓴다.
- 플레이스홀더가 필요하면 이미지가 아니라 `--line` 톤의 단색 블록을 쓴다.

> **이유**: 사진 한 장이 "직접 다녀왔다"를 증명하거나 무너뜨린다. 스톡 사진은 대부분 조명과 구도가 지나치게 완벽해서 오히려 티가 난다.

### 7.3 ❌ UI 유채색 금지

- 버튼·칩·배지·아이콘·링크에 유채색을 쓰지 않는다. 예외는 2.3의 폼 에러 하나뿐이다.
- 카테고리별·태그별 색상 부여를 하지 않는다.

### 7.4 ❌ 그림자 금지

- 카드·칩·버튼에 `box-shadow` 를 쓰지 않는다. `--bg`와 `--surface`의 명도 차이 + 보더로 충분하다.
- **유일한 예외**: 모달·바텀시트의 딤 배경.

### 7.5 ❌ 커머스 관용구 금지

- 할인·세일·인기·추천 뱃지, 카운트다운, "N명이 보는 중" 류의 문구를 쓰지 않는다.
- 가격을 강조하지 않는다. `priceRange` 는 표시하더라도 `caption` 크기의 보조 정보로 둔다.

> **이유**: 구조만 커머스에서 빌려오고 판촉 언어는 가져오지 않는다. 광고 없음이 이 서비스의 약속이다.

### 7.6 ❌ 명조 오용 금지

- 명조를 **28px 미만**에 쓰지 않는다.
- 명조를 버튼·칩·본문·캡션에 쓰지 않는다 (3.3의 세 곳만).
- 세로쓰기, 전통 문양, 먹 번짐, 낙관 등 전통 장식을 쓰지 않는다.

---

## 8. CSS 토큰

```css
:root {
  /* Color */
  --bg:         #F7F3EC;
  --surface:    #FFFDF8;
  --ink:        #2B2320;
  --ink-muted:  #635850;
  --line:       #E5DDD1;
  --on-ink:     #F7F3EC;
  --dim:        rgba(43, 35, 32, 0.45);

  /* Typography */
  --font-serif: "Noto Serif KR", serif;
  --font-body:  "Pretendard", -apple-system, sans-serif;

  /* Space (4px scale) */
  --space-1: 4px;   --space-2: 8px;   --space-3: 12px;
  --space-4: 16px;  --space-5: 20px;  --space-6: 24px;
  --space-8: 32px;  --space-12: 48px; --space-16: 64px;
  --space-24: 96px;

  /* Radius */
  --radius-sm:   8px;
  --radius-md:   10px;
  --radius-pill: 999px;

  /* Motion */
  --ease-out:        cubic-bezier(0.2, 0, 0, 1);
  --duration-enter:  500ms;  /* 섹션 스크롤 진입 */
  --duration-hover:  200ms;  /* 카드 호버 */
  --duration-state:  150ms;  /* 칩·버튼 상태 전환 */
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

---

## 9. 구현 체크리스트

- [ ] 화면에서 가장 채도가 높은 요소가 항상 사진인가
- [ ] 카드 사진과 카드 배경의 경계가 또렷하게 구분되는가
- [ ] UI 어디에도 유채색이 없는가 (폼 에러 제외)
- [ ] 별점·리뷰 수·통계 숫자가 화면 어디에도 없는가
- [ ] 모든 카드에 작성자와 방문일이 표시되는가
- [ ] 명조가 28px 미만에 쓰인 곳이 없는가
- [ ] 폰트 굵기가 400과 600 두 가지만 쓰였는가
- [ ] 결과 지도 영역에 움직이는 요소가 없는가
- [ ] 스크롤 애니메이션이 한 번만 실행되는가
- [ ] 칩이 44×44px 터치 영역을 확보했는가
- [ ] 칩이 가로 스크롤 없이 전부 보이는가
- [ ] 지도 결과에 대응하는 텍스트 리스트가 있는가
- [ ] `box-shadow` 가 딤 배경 외에 쓰이지 않았는가
- [ ] 320px 폭에서 레이아웃이 깨지지 않는가
- [ ] `prefers-reduced-motion` 에서 모션이 비활성화되는가

---

## 10. 후속 작업

1. **명조 폰트 확정** — Noto Serif KR 600 vs 고운바탕 실제 렌더링 비교. 리스크 C(한정식집 인상) 기준으로 판단한다.
2. **사진 촬영 가이드 수립** — 40장의 구도·조명·비율(4:3)을 통일한다. **사진이 제각각이면 이 디자인 전체가 무너진다.** PRD 열린 이슈 7번과 직결되며, 촬영 시작 전에 확정해야 한다.
3. **크림 배경과 실제 음식 사진 대비 실측** — 촬영본 5장을 카드에 올려 리스크 A를 검증한다. 필요 시 `--surface` 를 더 밝게 조정한다.
4. **폼 에러 색 1종 선정** — 2.3. 채도를 낮춘 벽돌색 계열에서 고른다.
5. **PRD 9장 갱신** — 모션 항목에 5장의 세부 규칙(한 번만 실행, 결과 이후 미사용)을 반영한다.

---

*이 문서는 인터뷰 기반 v1.0 초안이며, PRD.md v1.2의 디자인 관련 항목보다 우선한다.*
