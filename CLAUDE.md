# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

맛집 리뷰 서비스의 v1 랜딩페이지. **문서가 먼저 있고 코드가 그것을 구현하는 구조**다.

| 파일 | 역할 |
|---|---|
| `review_prd.md` | PRD v1.2 — 범위·기능·카피·데이터 스키마·리스크 |
| `review_design.md` | DESIGN v1.0 — 컬러·타이포·컴포넌트·금지사항·CSS 토큰 |
| `index.html` | 위 두 문서의 구현체. 단일 파일 (CSS·JS 인라인) |

**두 문서가 충돌하면 `review_design.md`가 이긴다.** 문서 말미에 "PRD.md v1.2의 디자인 관련 항목보다 우선한다"고 명시되어 있다.

## 명령어

빌드 · 번들러 · 테스트 프레임워크가 없다. 의존성도 없다.

```powershell
# 실행 — 그냥 파일을 연다
Start-Process .\index.html

# 로직 검증 — node 가 설치되어 있다 (v24). 임시 스크립트로 검사한다.
# index.html 에서 "const SERVICE_NAME" ~ "\ninit();" 구간을 잘라
# data: URL 로 import 하면 DOM 없이 순수 로직을 그대로 테스트할 수 있다.
# ($ / el 등 DOM 헬퍼는 정의만 되고 호출되지 않으므로 node 에서 안전하다.)
```

git 원격은 `origin` → `github.com/dongjin-star/sungshin_review` (public).

## index.html 구조

단일 파일이지만 세 층으로 나뉜다.

1. **`:root` 토큰** — `review_design.md` 8장을 그대로 옮긴 것. **값을 직접 고치지 말고 문서를 먼저 고친다.**
2. **`/* ===== SEED DATA ===== */` 블록** — 가게 데이터·카테고리·지역. PRD 9장이 "씨드 1곳 추가 = 1곳 수정"을 요구하므로 **데이터 수정은 전부 여기서만** 한다.
3. 그 아래 렌더링·인터랙션 로직.

### 알아둘 설계 결정 (코드만 봐서는 이유가 안 보이는 것들)

- **제외는 삭제가 아니라 하단 배치다.** `pickResults()` 는 선택된 카테고리를 `demote` 배열로 뒤로 밀 뿐 제거하지 않는다. 좁은 지역에서 완전 제외하면 결과가 0건이 되기 때문 (PRD 7.2 / 10.3).
- **지도는 외부 API를 쓰지 않는다.** lat/lng 바운딩 박스를 SVG viewBox에 선형 투영한다. 키·도메인 등록이 필요 없고 오프라인에서 동작한다. 카카오맵으로 교체하려면 `buildMap()` 하나만 갈아끼우면 된다.
- **지도는 결과 단계에서만 생성한다.** 첫 화면에 지도 SDK/DOM을 올리지 않는다는 PRD 9장 LCP 요건 때문이다.
- **`particle()` 은 장식이 아니다.** 제외 배너 문구는 DESIGN 6.5가 "서비스의 정체성을 전달하는 유일한 문장"으로 지정한 자리다. 받침 판정 없이 문자열을 이으면 `고기은` 같은 문장이 나온다.
- **위치 권한을 요청하지 않는다.** 거리는 사용자 위치가 아니라 지역 기준점(`REGIONS[].lat/lng`)에서 계산한다. 첫 방문에 권한 팝업을 띄우면 이탈한다는 PRD 7.3 결정.
- **`.js .reveal` 로 스코프된 이유** — JS가 죽으면 `opacity:0` 요소가 영영 안 보인다. `<head>`의 인라인 스크립트가 `html.js` 를 붙여야만 모션이 켜진다.
- **`[hidden]` 을 쓸 때 `display:flex` 를 조심한다.** `.cta-form[hidden]{display:none}` 이 명시적으로 필요했다.

## 어기면 안 되는 규칙

`review_design.md` 7장이 "권고가 아니라 규칙"으로 못박은 것들. 판단이 흔들리면 그 장을 읽는다.

- **UI에 유채색 금지.** 버튼·칩·배지·아이콘 전부 `--ink` / `--ink-muted` / `--line` 안에서 해결한다. 예외는 폼 에러용 `--error: #8C4A3F` 하나뿐이다.
- **정량 지표 표시 금지.** 별점·리뷰 수·방문자 수·저장 수·순위. v1의 리뷰는 1인이 쓴 40건이라 숫자를 적는 순간 카피가 거짓이 된다 (PRD 10.5). 단어로서의 "별점"(S5 소개문)은 허용, *표시*가 금지 대상이다.
- **`box-shadow` 금지.** 예외는 모달 딤 배경뿐.
- **명조는 28px 이상에서만.** 히어로 헤드 / S2 질문 / S4 CTA 세 곳 전용.
- **`font-weight` 는 400과 600만.**
- **스톡 이미지 금지.** 사진이 없으면 이미지 대신 `--line` 톤 단색 블록을 쓴다.
- **결과 지도 영역에 모션 금지.** 정보를 읽고 결정하는 화면이다.
- **스크롤 진입 모션은 한 번만.** 발동 후 `unobserve` 한다. 결과(S3)·CTA(S4) 에는 쓰지 않는다.

## 범위 잠금

PRD 4장이 **v1의 범위를 "랜딩페이지 1개"로 잠가뒀다.** 로그인·리뷰 작성·저장·리뷰 리스트·백엔드·DB·AI 요약·대시보드는 v1.1~v4 로드맵 항목이며, 요청받지 않은 한 만들지 않는다.

PRD 10.6이 특히 경고한다: AI 요약·대시보드는 지금 당장 만들 수 있고 만들고 싶어지지만, 리뷰 200건·가게당 3건이 쌓이기 전에 만들면 "기능이 있다는 증거"가 아니라 "데이터가 없다는 증거"가 된다.

## 미해결 상태 (배포 차단 항목)

1. **씨드 12곳이 가짜 데이터다.** 가게명·메모·방문일이 실재하지 않는다. 실촬영 데이터로 교체하기 전에는 공개하면 안 된다. `SEED DATA` 블록 상단에 경고가 있다.
2. **이메일이 수집되지 않는다.** `submitEmail()` 이 localStorage 에만 저장한다. 배포 시 이 함수 본문만 실제 엔드포인트로 교체한다.
3. **`og:image` 가 빈 경로다.** PRD 9장이 배포 첫날 처리를 요구한 항목.
4. **브라우저 시각 검증 미완료.** 로직·규칙은 정적으로 검증했으나 실제 렌더링·320px 레이아웃은 확인되지 않았다.

PRD 12장에 남은 열린 이슈(서비스명 확정, 씨드 지역·촬영 계획, 기술 스택)도 함께 참고한다. 현재 서비스명은 가제 `빼고`이며 `SERVICE_NAME` 상수 한 곳에서 관리된다.

## `onepage` 스킬과의 충돌

이 환경에는 랜딩페이지용 `onepage` 스킬이 있고, 메인 색상 `#9FB6A2`(세이지 그린)와 고정 섹션 순서(헤더→소개→특징3개→문의→푸터)를 규정한다. `review_design.md` 와 정면으로 충돌한다.

**해소 방식(사용자 승인됨): DESIGN.md 우선, onepage 스킬에서는 "HTML 파일 1개" 규칙만 채택.** 이 때문에 PRD 8장이 명시한 `data/places.js` 분리 대신 인라인 `SEED DATA` 블록을 쓴다.

## 반응형
- 모바일 (375)
- 태블릿 (768)
- 데스크토보 (1440)
으로 브레이크포인트 설정