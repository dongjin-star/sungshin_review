# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

맛집 검색 · 담기 앱. **문서가 먼저 있고 코드가 그것을 구현하는 구조**다.

> **2026-08-21 — 랜딩페이지를 삭제했다.** 사용자 요청으로 `index.html`(S1~S6 랜딩)과 `assets/app.js` 를 지우고
> `search.html` 을 `index.html` 로 올려 사이트 루트를 검색 앱으로 바꿨다. 삭제된 코드는 커밋 `40e938b` 에 남아 있다.
> 무엇이 같이 사라졌고 문서가 어디서 어긋나는지는 **PRD 4.2** 에 기록돼 있다 — 이 저장소를 처음 여는 사람은 그것부터 읽는다.

| 파일 | 역할 |
|---|---|
| `review_prd.md` | PRD — 범위·기능·카피·데이터 스키마·리스크. 4.1에 범위 개정 기록 |
| `review_design.md` | DESIGN — 컬러·타이포·컴포넌트·금지사항·CSS 토큰. 9장에 에어비앤비 채택 범위 |
| `CONTRACT.md` | 병렬 작업 계약 — 파일 소유권·클래스명·API 형태. 여러 명이 붙을 때 여기부터 읽는다 |
| `index.html` | 맛집 검색·담기. **사이트 루트이자 유일한 페이지다** |
| `assets/` | CSS·JS·씨드 데이터 |
| `api/` | 배포용 서버리스 함수 (`search`·`category`·`reviews`) + 공유 로직 `_lib/` |
| `server/proxy.mjs` | **개발 전용** 정적 서버. API 는 `api/` 의 같은 함수를 불러 쓴다 |

**두 문서가 충돌하면 `review_design.md`가 이긴다.** 문서 말미에 "PRD.md v1.2의 디자인 관련 항목보다 우선한다"고 명시되어 있다.

## 명령어

빌드 · 번들러 · 테스트 프레임워크가 없다. **npm 의존성도 0개다** (`package.json` 에 `dependencies` 가 없다).

```powershell
# 실행 — 파일을 직접 열면 안 된다. ES module 이라 file:// 에서 CORS 로 막힌다.
npm run dev          # → http://localhost:5173
```

카카오 검색을 실제로 쓰려면 `.env` 가 필요하다:
```powershell
Copy-Item .env.example .env    # 그 다음 KAKAO_REST_KEY 값을 채운다
```
**`.env` 없이도 전부 동작한다.** 프록시가 `503 NO_KEY` 를 주고 클라이언트가 씨드 12곳으로 폴백한다.

카카오 검색과 별개로 **구글 리뷰**를 쓰려면 키가 하나 더 필요하다:
```powershell
Add-Content .env "GOOGLE_MAPS_API_KEY=..."
```
**이것도 없어도 전부 동작한다.** `/api/reviews` 가 `503 NO_KEY` 를 주고 리뷰 영역에만 실패 문구가 뜬다 — 검색·담기는 그대로다.

```powershell
# 로직 검증 — package.json 이 "type":"module" 이라 .js 도 그대로 검사된다.
node --check assets\search.js
Get-ChildItem api,assets,server -Recurse -Include *.js,*.mjs | ForEach-Object { node --check $_.FullName }
```

git 원격은 `origin` → `github.com/dongjin-star/sungshin_review` (**public — `.env` 를 절대 커밋하지 않는다**).

## 파일 구조

원래는 `index.html` 단일 파일이었다. **두 명이 동시에 붙으면 한 파일에서 무조건 충돌**해서 2026-08-20에 쪼갰다. (그래서 `onepage` 스킬의 "HTML 파일 1개" 규칙은 더 이상 적용되지 않는다 — 아래 참조.)

| 파일 | 내용 |
|---|---|
| `assets/tokens.css` | `:root` 토큰. `review_design.md` 8장 대응. **값을 직접 고치지 말고 문서를 먼저 고친다** |
| `assets/components.css` | 전 컴포넌트. **색·타이포·보더는 전부 여기서만** 정의한다. 랜딩 전용 규칙(`.hero` `.quiz` `.card` `.cta-form` `.roadmap` …)이 아직 남아 있다 — 아래 참조 |
| `assets/seed.js` | 가게 데이터·카테고리·지역. PRD 9장이 "씨드 1곳 추가 = 1곳 수정"을 요구하므로 **데이터 수정은 전부 여기서만** |
| `assets/geo.js` | `metersBetween` · `walkMinutes(origin, target)` · `formatDate` |
| `assets/search.js` | 검색·필터·담기·리뷰 로직 |
| `assets/auth.js` | **로그인 세션·상태만. DOM 을 만지지 않는다** — 로그인 여부만 필요한 코드가 화면 코드를 끌고 오지 않게 |
| `assets/auth-ui.js` | 헤더 버튼 + 로그인 모달. `auth.js` 를 import 한다 |
| `assets/search.css` | 페이지 **레이아웃 전용**. 색·폰트·보더를 여기 쓰지 않는다 |
| `api/_lib/` | API 로직 전부. `http.js`(응답·`withErrors`) `errors.js`(오류 분류) `upstream.js`(외부 호출) `params.js` `normalize.js` `kakao.js` `places.js` + 엔드포인트별 `search·category·reviews.js` |
| `api/*.js` | Vercel 함수 진입점 3줄짜리. 로직은 `_lib/` 의 같은 이름 파일에 있다 |

`search.js` · `search.css` 는 이름을 그대로 뒀다. 페이지가 루트로 옮겨졌을 뿐 여전히 검색 화면의 자산이고,
`CONTRACT.md` 가 이 파일명으로 소유권을 규정하고 있어서 같이 바꾸면 계약 문서까지 흔들린다.

`<head>`의 `html.js` 인라인 스크립트만은 **인라인으로 남겨야 한다** (아래 설계 결정 참조).

**`components.css` 에 랜딩 전용 죽은 규칙이 남아 있다.** 지우지 않은 건 이 파일이 색·타이포·보더의 단일 출처라
검색 화면이 쓰는 규칙과 섞여 있고, 잘못 걷어내면 남은 화면이 조용히 깨지기 때문이다. 정리하려면
`index.html` 에 실제로 등장하는 클래스를 먼저 뽑아 대조한다.

### 알아둘 설계 결정 (코드만 봐서는 이유가 안 보이는 것들)

- **`pickResults()` · `buildMap()` · `particle()` 은 더 이상 없다.** 2026-08-21 랜딩과 함께 삭제됐다. 문서(PRD 7.2 / 10.3, DESIGN 6.5)는 아직 이 셋을 전제로 쓰여 있으니, 그 대목을 읽을 때 **코드에 없는 것**임을 알고 읽는다. 되살릴 일이 생기면 커밋 `40e938b` 의 `assets/app.js` 가 원본이다.
- **위치 권한을 첫 진입에 요청하지 않는다.** 거리는 기본적으로 지역 기준점(`REGIONS[].lat/lng`)에서 계산한다. 첫 방문에 권한 팝업을 띄우면 이탈한다는 PRD 7.3 결정. 단 7.3이 "현재 위치로 찾기" 버튼은 **명시적 클릭 시에만** 허용해뒀고, `index.html`의 `.search-geo`가 그 경로다. 클릭 전에는 `geolocation`을 건드리지 않는다.
- **검색 결과는 `.card` 가 아니라 `.result-card` 다.** DESIGN 6.3 / PRD 10.5가 `.card`에 작성자·방문일 표기를 **의무화**했는데 카카오 로컬 응답에는 둘 다 없다. 같은 카드로 렌더하면 "진짜 다녀온 사람의 기록"이라는 신뢰 카피가 그대로 깨진다. `source: "seed" | "kakao"` 가 구분자이고, `.result-card-origin` 줄이 출처를 밝힌다.
- **담기는 위시리스트다.** "다녀왔다"가 아니라 "가보고 싶다". PRD F9의 원래 정의와 같다. `localStorage["bbaego:wishlist"]` 에만 남으므로 기기를 바꾸면 사라진다 — F7(로그인) 도입 시 계정으로 이관해야 한다.
- **`region` 은 `null` 일 수 있다.** 기준점이 `REGIONS`에서 8km 넘게 떨어지면 `null`이다. 부산에서 "현재 위치로 찾기"를 쓴 결과에 "성수동"이 찍히면 메타 줄이 그대로 거짓말이 되기 때문. `null`이면 지역을 메타 줄에서 뺀다.
- **키워드 검색에는 반경을 보내지 않는다 — 전국이 기본이다.** 반경을 걸면 `"부산 돼지국밥"` 이 성수동 1km 밖이라는 이유로 **0건**이 된다(실측). 기준점(`x`/`y`)은 계속 보낸다: 카카오는 좌표만 있고 반경이 없으면 지역어가 없는 질의(`"파스타"`)를 기준점 근처로, 지역어가 있는 질의를 그 지역으로 알아서 넘긴다. 반대로 **카테고리 둘러보기(검색어 없음)는 반경이 본질**이라 계속 보낸다 — "근처에 뭐 있지"를 답하는 화면이고, 카카오 카테고리 검색은 반경이 0이면 아무것도 주지 않는다.
- **도보 분은 2km 이하에서만 쓴다** (`geo.js: distanceText`). 검색이 전국이 된 뒤로 기준점에서 300km 떨어진 결과가 들어오는데, 도보 분을 그대로 붙이면 "도보 4850분" 이 찍힌다. 숫자는 맞지만 문장이 거짓말이 된다. 2km 초과는 km 로 적는다. **`region` 이 `null` 이어서 메타 줄에 지역이 안 뜨는 먼 결과는 `.result-card-addr` 의 도로명이 위치를 알려주는 유일한 줄이다** — 그래서 주소 줄을 지우면 안 된다.
- **`#originNote` 는 기본 상태에서 비어 있다** (2026-08-21 변경). 첫 화면에 "성수동 기준 · 반경 1km" 를 띄우면 서비스 자체가 성수동 전용처럼 읽힌다 — 검색어를 치는 순간 전국이 되는데도 그렇다. 사용자가 **"현재 위치로 찾기"를 직접 눌러 기준점을 옮겼을 때만** 쓴다. 그 경우엔 안내가 아니라 "눌린 게 먹혔다"는 확인이라 없으면 피드백이 사라진다. 빈 `<p>` 의 margin 이 죽은 여백으로 남지 않게 `#originNote:empty{display:none}` 이 필요했다.
  - **기본 기준점이 여전히 성수동이라는 사실은 그대로다.** 문구만 감춘 것이지 동작이 바뀐 게 아니다. 둘러보기(검색어 없음)는 `REGIONS[0]` 반경 안에서 돈다.
- 0건 화면의 "N km까지 넓혀서 찾기" 버튼은 **둘러보기 모드에서만** 낸다. 검색 모드는 이미 전국이라 눌러도 결과가 그대로다.
- **검색은 카카오 3페이지를 다 긁는다** (`MAX_PAGES`). 카카오 로컬은 `size=15 × page 3 = 45곳`이 상한인데, 1페이지만 받으면 반경 안에 45곳이 있어도 15곳만 손에 들어온다. 칩 필터는 **클라이언트에서** 그 손에 든 목록을 거르므로, 1페이지만 받으면 "일식 0건" 같은 **거짓 0건**이 나온다 (성수동 1km 실측: 일식 page1 0 / 3페이지 5, 고기 2 / 10). 1페이지는 받는 즉시 그리고 나머지는 뒤에서 붙이되, 칩이 걸려 있고 1페이지 결과가 0건이면 그리지 않고 로딩을 유지한다 — 곧 결과로 덮일 "없어요"가 깜빡이기 때문.
- **API 로직은 한 벌뿐이다.** `api/_lib/` 에 있고, 배포는 `api/*.js`(Vercel 함수)가, 로컬은 `server/proxy.mjs` 가 **같은 함수 객체**를 부른다. `proxy.mjs` 가 핸들러가 아니라 `api/search.js` 를 통째로 import 하는 게 그 장치다 — `withErrors` 의 버그가 로컬에서 드러나야 한다. 한쪽에만 있는 경로를 만들면 로컬에서 통과한 게 배포에서 깨진다.
- **핸들러는 `res` 를 모른다.** 페이로드를 `return` 하면 `withErrors` 가 내보낸다. 그래서 로직에 HTTP 의존이 없고, 두 런타임의 차이(쿼리 꺼내는 법)는 `paramsFrom()` 한 곳에 갇힌다.
- **`/api/reviews` 는 좌표를 필수로 받는다** (`readOrigin`, `readLocation` 아님). `readLocation` 은 좌표가 없으면 조용히 성수동으로 되돌리는데, 리뷰 조회에서 그 폴백이 걸리면 부산 가게를 성수동 기준으로 150m 검사해서 **"구글에 그런 가게 없어요"라는 거짓 답**을 자신 있게 돌려준다. 틀린 답보다 400 이 낫다.
- **구글 `locationBias` 는 제한이 아니라 편향이다.** 150m 원을 줘도 근처에 없으면 수 km 밖 결과를 그대로 준다. 그래서 서버가 `metersBetween` 으로 다시 재서 150m 안만 남긴다 (`api/_lib/reviews.js: pickNearest`). 이 검사를 빼면 "전국에 같은 이름이 여러 개" 요구사항이 그대로 무너진다. (`locationRestriction` 은 사각형만 받고 카테고리 질의 전용이라 못 쓴다.)
- **리뷰 캐시는 세션 메모리다.** 구글 약관이 리뷰·별점 캐싱을 금지하고 **`place_id` 만 무기한 저장을 허용**한다. 그래서 리뷰 본문은 탭이 살아 있는 동안만 `Map` 에 두고 `localStorage` 에는 `place_id` 만 남긴다(`bbaego:googlePlaceId`). 응답에 `Cache-Control: no-store` 를 유지하는 것도 같은 이유다 — 엣지 캐시가 붙으면 약관 위반이다.
- **리뷰 실패를 `requestPage` 쪽 오류 처리로 넘기면 안 된다.** 그쪽은 `503 NO_KEY` 를 보면 페이지 전체를 씨드 폴백 + "샘플로 둘러보는 중" 배너로 뒤집는다. **구글** 키가 없다는 이유로 카카오 검색 결과가 통째로 샘플로 바뀌면 그 배너가 거짓말이 된다. 리뷰 실패는 패널 안에서만 처리한다.
- **리뷰 본문을 자르지 않고 작성자·원본 링크·정렬 고지를 같이 낸다.** UI 취향이 아니라 구글 정책 요구사항이다. `line-clamp` 를 넣는 순간 위반이다.
- **리뷰는 카드 안이 아니라 모달이다** (2026-08-21 변경). 처음엔 카드에서 펼쳤는데, 구글 정책상 리뷰 원문을 자를 수 없어 카드가 화면 몇 개 높이로 길어져 목록을 훑을 수가 없었다. 네이티브 `<dialog>` + `showModal()` 을 쓰면 포커스 가둠·Esc·배경 비활성화·`::backdrop` 을 브라우저가 해 준다 — 의존성 0 을 지키면서 직접 만들면 반드시 빠뜨리는 것들이다. 딤 배경 클릭 닫기만 직접 붙였다.
- **`.result-grid` 의 `align-items: start`** 는 펼침 UI 시절의 잔재지만 그대로 둔다. 카드 높이가 제각각일 때 아래쪽 빈 공간이 안 생기는 게 그 자체로 낫다.
- **모달 닫기 버튼에는 JS 리스너가 없다.** `<form method="dialog">` 안의 submit 버튼이라 브라우저가 닫는다. `wireModal()` 이 아예 안 불려도 닫기는 동작한다 — 모달에 갇히는 것이 최악의 실패라 그 경로만은 스크립트에 걸지 않았다. 딤 배경 클릭만 JS 다.
- **모달 닫기 버튼은 44×44 다.** 32px 로 만들었다가 키웠다 — 이 시스템의 다른 컨트롤은 전부 44~48px(`.filter-chip` 44 · `.search-input` 48)인데 그것만 아래였다. 닫는 유일한 버튼이라 한 번에 안 눌리면 "느리게 닫힌다"로 체감된다. 실측으로는 세 경로(X·딤 클릭·Esc) 전부 0~6ms 다.
- **열기 버튼의 밑줄을 `--ink-muted` 로 올렸다.** `--line` 으로 그었더니 배경과 톤이 거의 같아 화면에서 보이지 않았다. 색을 못 쓰는 팔레트라 밑줄이 "누를 수 있다"는 **유일한** 신호다.
- **Supabase publishable 키만은 클라이언트에 그대로 있다** (`assets/auth.js` 상단). 위의 "키는 브라우저로 안 나간다" 규칙의 **유일한 예외**이고 사고가 아니다. 차이는 키가 무엇을 허가하느냐다 — 카카오·구글 키는 **소지자가 곧 권한**이라 새면 남의 계정으로 과금된다. `sb_publishable_…` 은 "이 요청은 anon 역할" 이라는 **신분 표시**일 뿐이고, 실제로 무엇을 할 수 있는지는 서버의 RLS 가 정한다. 브라우저에 넣으라고 만든 키다.
  - **그래서 담기를 Supabase 테이블로 옮기는 순간 RLS 가 필수가 된다.** RLS 없이 테이블을 열면 이 공개 키로 전 세계가 읽고 쓴다. 조용히, 오류 없이 열린다. 지금은 테이블이 없어 위험이 없지만 다음 작업의 1번이다.
  - `service_role` / `secret` 키는 절대 `assets/` 에 넣지 않는다. 필요하면 `api/_lib/` 와 `.env` 로 간다.
- **로그인이 생기면서 XSS 의 대가가 달라졌다.** 전에는 화면이 망가지는 정도였지만 이제 리프레시 토큰이 새어 계정이 넘어간다. `search.js`·`auth-ui.js` 가 `innerHTML` 을 한 번도 안 쓰고 `textContent`·`createElement` 만 쓰는 것은 이제 **취향이 아니라 보안 불변식**이다.
- **세션은 `localStorage["bbaego:session"]`.** httpOnly 쿠키를 쓰려면 서버 세션이 필요한데, 범위 잠금이 `server/proxy.mjs` 를 "키를 가리는 프록시 한 개, DB도 세션도 없다"로 못박아 뒀다. 감수한 선택이지 실수가 아니다.
- **`currentUser()` 는 동기다.** 세션이 `localStorage` 라 모듈 로드 시점에 복원된다. 담기 게이트를 `if (!currentUser()) …` 한 줄로 쓸 수 있게 하려는 것이다. `onAuthChange(fn)` 는 **구독하는 즉시 현재 값으로 한 번 부른다** — "구독 전에 이벤트가 이미 지나갔다" 는 버그 종류를 없앤다.
- **토큰 갱신에서 일시적 장애로 로그아웃시키지 않는다.** 명시적 폐기 코드(`REVOKED` 집합)일 때만 세션을 지운다. 네트워크·5xx·429·모르는 코드는 세션을 지키고 다음 기회를 노린다. 카페 와이파이가 502를 뱉었다고 사용자를 내쫓으면 안 된다. 갱신 요청은 **한 번에 하나** — 리프레시 토큰은 1회용이라 동시에 두 번 쓰면 둘 다 죽는다. 다른 탭이 먼저 회전시킨 경우는 저장된 값을 다시 읽어 받아들인다.
- **로그아웃은 `?scope=local` 이다.** 기본값 `global` 은 그 사용자의 **모든 기기** 세션을 폐기한다 — 노트북에서 로그아웃했는데 폰에서도 풀린다.
- **카카오 REST 키는 `api/_lib/kakao.js`, 구글 키는 `api/_lib/places.js` 안에서만 읽힌다.** 브라우저로 나가지 않는다 (PRD 12장 이슈 8). `.env`는 `.gitignore` 대상이고 **저장소가 public이라 이건 타협 불가**다. 키가 없으면 프록시가 `503 NO_KEY`를 주고 클라이언트는 씨드 12곳으로 폴백한다 — 폴백은 예외 경로가 아니라 기본 경로다.
- **`.js .reveal` 로 스코프된 이유** — JS가 죽으면 `opacity:0` 요소가 영영 안 보인다. `<head>`의 인라인 스크립트가 `html.js` 를 붙여야만 모션이 켜진다. 지금 남은 화면에는 `.reveal` 요소가 없지만, 인라인 스크립트와 CSS 규칙은 그대로 뒀다 — 다시 섹션을 붙일 때 이 스코프가 없으면 같은 함정을 다시 밟는다.
- **`[hidden]` 을 쓸 때 `display:flex` 를 조심한다.** 삭제된 랜딩에서 `.cta-form[hidden]{display:none}` 이 명시적으로 필요했다. 지금 화면의 `#demoBanner` · `#savedEmpty` 도 같은 함정 위에 있다.
- **`<dialog>` 도 같은 함정이다 — 여기서 실제로 밟았다.** `.review-modal { display: flex }` 를 쓰는 순간 브라우저 기본값(닫히면 `display:none`)이 무력화되어 **닫아도 화면에 그대로 남는다.** `dlg.open` 은 `false` 인데 눈에는 보이는 상태라, `open` 속성만 확인하는 테스트는 전부 통과한다. `.review-modal:not([open]) { display: none; }` 이 반드시 필요하다.
  - 교훈: 숨김을 검증할 때 **상태 플래그가 아니라 `getBoundingClientRect().height` 를 본다.**
- **`<dialog>` 의 `close` 이벤트를 믿지 않는다.** Chrome 151 에서 `close` 도 `cancel` 도 발생하지 않는 것을 실측했다 — 우리 코드 문제가 아니라 방금 만든 빈 `dialog` 도 마찬가지였다. 그래서 **정리는 닫을 때가 아니라 열 때 한다** (`openAuthModal()` 이 `form.reset()` + `setMode("login")`). 닫을 때 걸면 다음에 열었을 때 지난 입력값과 모드가 그대로 남는다. `close` 리스너는 덤으로만 남겨 둔다.
  - 같은 이유로 `search.js` 의 리뷰 모달은 `modalPlaceId` 대신 **`dlg.open` 을 직접 보고** 늦은 응답을 거른다.

## 어기면 안 되는 규칙

`review_design.md` 7장이 "권고가 아니라 규칙"으로 못박은 것들. 판단이 흔들리면 그 장을 읽는다.

- **UI에 유채색 금지.** 버튼·칩·배지·아이콘 전부 `--ink` / `--ink-muted` / `--line` 안에서 해결한다. 예외는 폼 에러용 `--error: #8C4A3F` 하나뿐이다.
- **정량 지표 표시 금지 — 우리 데이터에 한해** (2026-08-21 개정). 별점·리뷰 수·방문자 수·저장 수·순위. v1의 리뷰는 1인이 쓴 40건이라 숫자를 적는 순간 카피가 거짓이 된다 (PRD 10.5).
  - 예외 ①: 내가 담은 목록의 개수
  - 예외 ②: **구글 리뷰 패널 안의 구글 별점·리뷰 수.** 남의 표본이라 위 근거가 걸리지 않는다. 단 **출처가 화면에 드러나는 자리에서만** — 카드 메타 줄로 끌어내거나 정렬·순위 기준으로 쓰는 것은 여전히 금지다 (그게 원래 막으려던 행동이다).
- **UI에 유채색 금지 — 예외는 둘.** 폼 에러 `--error`, 그리고 구글 리뷰 패널 안의 `⭐` 글리프. ⭐ 는 패널 밖으로 나가지 않는다.
- **`box-shadow` 금지.** 예외는 모달 딤 배경뿐.
- **명조는 28px 이상에서만.** 원래 히어로 헤드 / S2 질문 / S4 CTA 세 곳 전용이었고, 그 세 곳은 랜딩과 함께 사라졌다. **지금 화면에는 명조를 쓸 자리가 없다.**
- **`font-weight` 는 400과 600만.**
- **스톡 이미지 금지.** 사진이 없으면 이미지 대신 `--line` 톤 단색 블록을 쓴다.
- **결과 지도 영역에 모션 금지.** 정보를 읽고 결정하는 화면이다.
- **스크롤 진입 모션은 한 번만.** 발동 후 `unobserve` 한다. 결과 목록에는 쓰지 않는다 — 읽고 결정하는 화면이다.

## 범위 잠금

**2026-08-21 — 로그인(F7)을 앞당겼다.** 사용자 요청. 지금 v1 = 검색 · 담기 · **로그인** 이다. 개정 기록은 PRD 4.4.

**나머지 잠금은 그대로다.** 리뷰 작성(F8) · DB · AI 요약 · 대시보드 · 취향 온보딩은 여전히 v2~v4 이며 요청받지 않은 한 만들지 않는다. **담기는 아직 `localStorage` 다** — 계정으로 옮기는 것은 별도 작업이고, 지금 로그인은 "헤더에 이름이 뜨는 것" 외의 효과가 없다. 의도된 중간 상태다.

PRD 4장이 v1 범위를 "랜딩페이지 1개"로 잠가뒀었다. **2026-08-20에 검색(F6)과 담기(F9)를 v1으로 앞당겼고(PRD 4.1), 2026-08-21에 랜딩 자체를 삭제했다(PRD 4.2).** 지금 v1 = 검색 · 담기 두 기능이 전부다.

**나머지 잠금은 그대로 유효하다.** 로그인·리뷰 작성·DB·AI 요약·대시보드·취향 온보딩은 여전히 v2~v4 항목이며, 요청받지 않은 한 만들지 않는다. `server/proxy.mjs` 는 백엔드가 아니라 **키를 가리는 프록시 한 개**다 — DB도 세션도 없고, 그 이상으로 키우지 않는다.

PRD 10.6이 특히 경고한다: AI 요약·대시보드는 지금 당장 만들 수 있고 만들고 싶어지지만, 리뷰 200건·가게당 3건이 쌓이기 전에 만들면 "기능이 있다는 증거"가 아니라 "데이터가 없다는 증거"가 된다.

## 미해결 상태 (배포 차단 항목)

1. **씨드 12곳이 가짜 데이터다.** 가게명·메모·방문일이 실재하지 않는다. 실촬영 데이터로 교체하기 전에는 공개하면 안 된다. `assets/seed.js` 상단에 경고가 있다.
2. **이메일 수집 경로가 아예 없다.** 2026-08-21 랜딩 삭제로 F5(출시 알림 이메일) 폼과 `submitEmail()` 이 같이 사라졌다. 출시 알림을 받을 방법이 없으므로, 필요하면 새로 만들어야 한다 (PRD 4.2).
3. **`og:image` 가 빈 경로다.** PRD 9장이 배포 첫날 처리를 요구한 항목. OG 블록은 랜딩에서 새 `index.html` 로 옮겨왔고 `og:url` 도 `https://example.com/` 그대로다.
4. ~~카카오 실키 경로가 미검증이다.~~ **2026-08-21 해소.** 실키로 `/api/search`·`/api/category` 를 확인했다. `meta.isEnd` 는 page 3 에서 `true` 로 오고(45곳이 상한), `mapCategory()` 는 성수동 45곳 중 35곳을 매핑했다. 못 매핑한 10곳은 전부 `간식`(도넛·베이커리·아이스크림)과 `술집` — `CATEGORIES` 에 대응 코드가 아예 없는 분류라 오작동이 아니다.
5. **담은 목록이 기기에 묶여 있다.** `localStorage` 뿐이라 기기를 바꾸면 사라진다. F7(로그인) 도입 시 이관 작업이 필요하다.
6. **실기기 검증 미완료.** 320/375/768/1440은 동일 오리진 iframe 프로브로 실측했으나 실제 모바일 기기에서는 확인하지 않았다.
7. **카카오 "치킨" 분류에 대응 코드가 없다.** `category: null` 로 두어 칩 필터에서 빠진다. 2026-08-21 실응답 확인 — "치킨"으로 검색하면 15곳 전부 `category: null` 이다. 카드에는 `categoryLabel` 로 "치킨"이 그대로 보이지만 어떤 칩으로도 걸러지지 않는다. `간식`·`술집` 도 같은 상태다. `CATEGORIES` 에 추가할지는 제품 결정이라 보류 중 (PRD 12장 이슈 4가 "씨드 입력 전 고정"으로 잠가둔 목록).

9. **구글 리뷰의 실제 매칭률이 미검증이다.** 150m 필터가 얼마나 자주 헛치는지는 성수동 표본으로만 봤다. 신규·소규모 가게는 구글에 없거나 이름이 달라 `found:false` 가 잦을 수 있다. 잦으면 `address` 파라미터 활용을 늘리거나 반경을 재검토한다.
10. **리뷰 조회 비용이 클릭당 최상위 등급이다.** `reviews`·`rating`·`userRatingCount` 는 Places 의 Enterprise + Atmosphere 등급이고 **한 요청에 걸린 가장 높은 등급으로 과금**된다. 세션 캐시가 유일한 방어선이라 빼면 안 된다. 구글 클라우드 콘솔에서 예산 알림을 걸어두는 것을 권한다.
11. **Vercel 환경변수가 아직 안 들어갔을 수 있다.** `KAKAO_REST_KEY` 와 `GOOGLE_MAPS_API_KEY` 를 대시보드(Settings → Environment Variables)에 넣어야 배포본이 동작한다. `.env` 는 git 에 안 올라가므로 배포에는 없다. 대시보드 설정도 확인: Framework Preset = **Other**, Build Command·Output Directory 는 **비워 둔다** (Output 을 `public` 으로 두면 사이트 전체가 404).

12. **Supabase 의 "Confirm email" 이 켜져 있다** (`mailer_autoconfirm: false`). 이 상태로는 요청받은 **"회원가입하면 바로 로그인"이 동작하지 않는다** — 가입 시 세션이 안 나오고 확인 메일이 발송된다. 대시보드 → Authentication → Sign In / Providers → Email → **Confirm email 끄기**. 켜둔 채로 쓰려면 Site URL·Redirect URLs 도 등록해야 메일 링크가 돌아온다 (`adoptHashSession()` 이 그 경로를 받는다).
    - **설정을 꺼도 그 전에 만들어진 미확인 계정은 자동으로 확인되지 않는다.** 테스트 중 생긴 계정은 대시보드에서 지우거나 수동 확인해야 하고, 안 그러면 "로그인이 고장났다"로 보인다.
13. **RLS 정책이 하나도 없다.** 지금은 테이블이 없어 위험이 없지만, 담기를 Supabase 로 옮기기 전에 **반드시** 먼저 건다. `enable row level security` + select/insert/update/delete 네 정책, insert·update 에는 `with check` 까지. 빠뜨리면 공개 키로 전 세계가 읽고 쓴다.
14. **담기가 아직 계정에 묶이지 않았다.** 로그인해도 담은 목록은 `localStorage` 그대로다. 이관 시 `onAuthChange` 의 로그인 시점에서만 1회 실행해야 한다 — 매 로드마다 도는 `restore` 에서 돌리면 안 된다.

PRD 12장에 남은 열린 이슈(서비스명 확정, 씨드 지역·촬영 계획, 기술 스택)도 함께 참고한다. 현재 서비스명은 가제 `빼고`이며 `seed.js` 의 `SERVICE_NAME` 상수 한 곳에서 관리된다 — **다만 랜딩 삭제 후 이 상수를 읽는 코드가 없다.** 화면의 "빼고"는 `index.html` 에 하드코딩돼 있다.

8. **서비스명의 근거가 사라졌다.** `빼고` 는 "어제 먹은 것을 뺀다"는 F1 제외 로직에서 나온 이름인데, 그 기능이 2026-08-21에 삭제됐다. 지금 앱은 빼지 않고 찾기만 한다. PRD 12장 이슈 1(서비스명 확정)에 이 사유를 얹어 다시 판단해야 한다.

## `onepage` 스킬과의 충돌

이 환경에는 랜딩페이지용 `onepage` 스킬이 있고, 메인 색상 `#9FB6A2`(세이지 그린)와 고정 섹션 순서(헤더→소개→특징3개→문의→푸터)를 규정한다. `review_design.md` 와 정면으로 충돌한다. **2026-08-21 랜딩 삭제로 이 저장소에는 스킬이 규정하는 랜딩페이지 자체가 없다** — 충돌은 사실상 무의미해졌지만, 다시 랜딩을 만들 때를 위해 해소 방식은 남겨둔다.

**해소 방식(사용자 승인됨): DESIGN.md 우선.**

~~onepage 스킬에서 "HTML 파일 1개" 규칙만 채택~~ — **2026-08-20 폐기.** 두 명이 동시에 작업하려면 한 파일로는 불가능해서 `assets/` 로 쪼갰다. 씨드 데이터는 인라인 블록이 아니라 `assets/seed.js` 에 있다 (PRD 8장이 원래 요구한 분리 구조에 오히려 가까워졌다).

## 반응형

브레이크포인트는 **375 / 768 / 1440** 이다. 하한은 320px 이며 여기서 레이아웃이 깨지면 안 된다 (PRD 9장).

분해 전 코드에는 599/600/900/1200 이 뒤섞여 있었으나 2026-08-20에 정리했다. 새 미디어쿼리를 추가할 때 이 세 값 밖으로 나가지 않는다.

## 여러 명이 동시에 작업할 때

`CONTRACT.md` 를 먼저 읽는다. 파일 소유권 표가 거기 있다. 핵심은 **색·타이포·보더는 `components.css` 한 곳에서만** 정의하고, 페이지별 CSS(`search.css`)는 레이아웃 선언만 갖는다는 것이다. 이 경계는 규칙일 뿐 강제되지 않으므로 grep 으로 확인한다.