# 맛집 리뷰 서비스 — PRD

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.2 |
| 작성일 | 2026-08-14 |
| 수정일 | 2026-08-14 (기능 목록 확장 — F3~F11 추가, 로드맵 재배치) |
| 작성자 | JJIn |
| 상태 | Draft (랜딩페이지 착수 전) |
| 서비스명 | **미정** (12장 열린 이슈 1번) |

---

## 1. 개요

### 1.1 한 문장 정의

> **처음 가는 동네에서 오늘 뭘 먹을지 정하지 못한 사람**이, **어제 먹은 메뉴를 알려주면**, **그것과 겹치지 않는 근처 맛집을 지도에서 받아보는** 서비스다.

| 축 | 내용 |
|---|---|
| **누가** | 낯선 동네에 있거나, 늘 가던 곳에 질린 사람 |
| **언제** | 식사 직전 30분. 배는 고픈데 후보가 안 떠오르는 순간 |
| **왜** | 검색은 광고를 주고, 지도는 별점을 주는데, **"어제 먹은 것과 다른 것"** 은 아무도 안 걸러주기 때문 |

### 1.2 배경 — 왜 추천이 아니라 제외인가

기존 서비스는 전부 **더하는 방식**이다. 별점이 높은 곳, 리뷰가 많은 곳, 광고비를 낸 곳을 쌓아 올린다. 그래서 결과가 매번 비슷하고, 어제 먹은 김치찌개가 오늘도 상단에 뜬다.

이 서비스는 **빼는 방식**을 택한다. 질문 하나("어제 뭐 드셨어요?")로 오늘의 후보군에서 한 덩어리를 덜어낸다. 사용자가 하는 일은 취향을 설명하는 게 아니라 **기억 하나를 말하는 것**이라 진입 부담이 낮다.

### 1.3 콜드스타트를 우회하는 전략

리뷰 서비스의 구조적 난점은 명확하다. **리뷰가 없으면 추천할 게 없고, 추천할 게 없으면 아무도 안 와서 리뷰가 안 쌓인다.**

따라서 순서를 뒤집는다.

```
[탐색 먼저 만들기]   빈 화면 → 이탈 → 데이터 영원히 안 쌓임   ❌
[기록 먼저 만들기]   혼자 써도 쓸모 있음 → 사용자가 데이터를 채움 → 그 데이터로 탐색 개방   ✅
```

**v1~v2는 "내 맛집 기록장"이다.** 남의 리뷰 없이도 혼자 쓸 수 있어야 한다. 기록이 임계치를 넘으면 그때 탐색·추천을 연다.

단, 랜딩의 첫인상까지 비워둘 수는 없으므로 **운영자가 씨드 데이터 30~50곳을 직접 입력**해 출시 첫날부터 지도에 결과가 뜨게 한다. (→ 10.1 리스크)

### 1.4 성공의 정의 (v1 랜딩 기준)

v1은 트래픽이 아니라 **"이 질문에 사람들이 답을 하는가"** 로 평가한다. 이게 안 되면 서비스의 전제가 틀린 것이다.

| 지표 | 목표 | 의미 |
|---|---|---|
| 질문 시작률 (방문 → 첫 응답) | 40% | 이 훅이 통하는가 |
| 질문 완료율 (시작 → 결과 도달) | 70% | 흐름이 지루하지 않은가 |
| 결과 도달 후 전환 (이메일 or 기록 시도) | 15% | 결과가 값어치 있었는가 |
| **지도 결과 0건 비율** | 10% 이하 | 씨드 데이터 커버리지 |
| 모바일 레이아웃 깨짐 | 0건 | — |

> 목표 수치는 검증되지 않은 가설이다. 출시 2주 후 실측값으로 갱신한다.

---

## 2. 타겟 사용자

### 2.1 낯선 동네의 한 끼 (1순위)
- **맥락**: 출장·약속·이사 등으로 모르는 지역에 있다. 시간은 20분, 도구는 휴대폰 하나.
- **원하는 것**: 실패하지 않을 선택지 3~5개. 100개가 아니다.
- **설계 시사점**: 모바일 우선. 위치 입력이 번거로우면 그 자리에서 이탈한다.

### 2.2 중요한 자리를 준비하는 사람 (2순위)
- **맥락**: 데이트·상견례·회식. 미리 찾아보고 저장해둔다. 데스크톱 비중이 있다.
- **원하는 것**: 실패 확률이 낮다는 근거. 분위기와 상황 적합성.
- **설계 시사점**: **저장 기능이 이 그룹의 핵심**이다. 상황 태그(데이트·회식·부모님)의 주 소비자.

### 2.3 다녀온 곳을 기록하는 사람 (3순위, 그러나 v1의 실사용자)
- **맥락**: 좋았던 가게를 잊어버린 경험이 있다. 스스로 목록을 쌓고 싶어 한다.
- **원하는 것**: 빠른 입력. 사진 한 장 + 태그 몇 개로 끝나야 한다.
- **설계 시사점**: 우선순위상 3순위지만, **초기 데이터를 만들어주는 사람은 이 그룹뿐이다.** v1~v2의 실질적 주인공.

### 2.4 유저 스토리

- 방문자로서, 나는 가입 없이 질문 하나에 답하고 바로 결과를 보고 싶다. 지금 배가 고프기 때문이다.
- 방문자로서, 나는 어제 먹은 것과 겹치지 않는 곳만 보고 싶다. 후보를 좁히는 게 목적이기 때문이다.
- 준비하는 사람으로서, 나는 괜찮아 보이는 곳을 눌러 저장해두고 싶다. 지금 가는 게 아니라 다음 주에 가기 때문이다.
- 기록하는 사람으로서, 나는 다녀온 가게를 1분 안에 남기고 싶다. 길면 안 쓰게 되기 때문이다.
- 운영자로서, 나는 씨드 데이터를 코드 수정 없이 한 곳에서 추가하고 싶다. 30~50번 반복할 작업이기 때문이다.

---

## 3. 핵심 가설

이 서비스는 아래 3개 가설 위에 서 있다. 랜딩은 **가설 1**을 검증하기 위한 장치다.

| # | 가설 | 검증 시점 | 검증 방법 |
|---|---|---|---|
| 1 | 사람들은 "어제 뭐 먹었나"라는 질문에 기꺼이 답한다 | **v1 랜딩** | 질문 시작률·완료율 |
| 2 | 겹치지 않는 결과는 일반 추천보다 만족스럽다 | v2 | 결과 클릭률, 재방문 |
| 3 | 사용자는 남이 안 봐도 자기 기록을 쌓는다 | v2 | 주당 기록 수, 4주 잔존율 |

> 가설 1이 깨지면 제외 로직 자체를 재검토해야 한다. 그래서 랜딩을 먼저 만드는 것이 맞다.

---

## 4. 범위 — 오늘 만드는 것

> ### 🔓 **2026-08-20 개정 — 범위 잠금을 풀었다.**
> 아래 원래 표의 오른쪽 열에 있던 **검색(F6, v1.1)** 과 **담기(F9, v2)** 를 사용자 요청으로 v1에 앞당겼다.
> 개정 내역은 이 절 끝의 **4.1** 에 있다. 나머지 항목의 잠금은 그대로 유효하다.

| 만든다 (v1) | 만들지 않는다 |
|---|---|
| 랜딩페이지 1개 (단일 페이지) | 회원가입 / 로그인 (Supabase) |
| "어제 뭐 드셨어요?" 질문 인터랙션 | 리뷰 작성 기능 |
| 제외 결과 지도 표시 | ~~저장·위시리스트~~ → **v1로 앞당김 (4.1)** |
| 씨드 데이터 30~50곳 (운영자 입력) | ~~리뷰 리스트 + 검색·필터 → v1.1~~ → **v1로 앞당김 (4.1)** |
| 이메일 수집 CTA | AI 리뷰 요약·감성 분석 (Gemini) |
| **스크롤·호버 애니메이션** | 대시보드 (도넛·워드클라우드) |
| **모바일 최적화 (320px~)** | 취향 온보딩 / 취향 기반 정렬 |
| **배포 · 도메인 · SEO 메타** | ~~백엔드 API~~ → **키 프록시만 추가 (4.1)** / 데이터베이스 |
| **맛집 검색 · 담기 페이지 (`search.html`)** | 네이티브 앱 |

**v1의 데이터는 정적 파일 하나로 충분하다.** 30~50개 항목에 DB는 과잉이다.
이 판단은 **씨드 데이터에 한해** 여전히 유효하다. 카카오 로컬 결과는 저장하지 않고 조회 시점에만 쓴다.

### 4.1 범위 개정 기록 (2026-08-20)

| 항목 | 원래 | 개정 | 사유 |
|---|---|---|---|
| F6 검색·필터 | v1.1 | **v1** | 사용자 요청 |
| F9 저장(담기) | v2 | **v1** | 사용자 요청 |
| 백엔드 API | 만들지 않음 | **키 프록시 1개만** (`server/proxy.mjs`) | 아래 참조 |
| 지도 API | 미정 (12장 이슈 5) | **카카오 로컬 확정** | 국내 장소 데이터 |

**F6는 원래 "JSON 정적 데이터 기반"이었으나 카카오 로컬 API 기반으로 바뀌었다.** 이 변경의 부작용:

1. **정적 호스팅으로는 배포할 수 없다.** 프록시가 Node 실행 환경을 요구한다. GitHub Pages 불가.
   프록시는 12장 이슈 8("API 키를 클라이언트에 노출하지 않는다. 서버 라우트를 경유한다")을 지키기 위한 최소 구성이며,
   DB도 세션도 없다. 카카오 응답을 정규화해 넘기는 것 외에 아무 상태도 갖지 않는다.
2. **F9(담기)를 F7(로그인·Supabase)보다 먼저 만들게 됐다.** 5.1의 의존 사슬을 거스르는 것이다.
   이 때문에 담긴 목록은 서버가 아니라 **브라우저 `localStorage`** 에만 남는다. 기기를 바꾸면 사라진다.
   F7 도입 시 이 데이터를 계정으로 이관하는 작업이 별도로 필요하다.
3. **검색 결과는 "다녀온 기록"이 아니다.** 카카오 로컬 응답에는 작성자·방문일·실촬영 사진이 없다.
   6.3과 10.5가 카드에 작성자·방문일 표기를 의무화했으므로, 검색 결과는 씨드 카드(`.card`)와
   **다른 컴포넌트**(`.result-card`)로 렌더하고 출처를 명시한다. 8장 스키마의 `source` 필드가 구분자다.
   담기는 "다녀왔다"가 아니라 **"가보고 싶다"(위시리스트)** 로 정의한다 — F9의 원래 정의와 같다.

---

## 5. 핵심 기능 목록 (완성형 기준)

완성형 서비스가 가져야 할 기능 전체 목록이다. **v1 열을 보면 오늘의 범위가 명확해진다.**

**유형** — `기능` 사용자가 직접 쓰는 것 / `기반` 다른 기능을 떠받치는 것 / `품질` 특정 화면에 속하지 않고 전체에 걸리는 것

| # | 기능 | 유형 | 설명 | v1 | 도입 단계 |
|---|---|---|---|---|---|
| F1 | **제외 질문 인터랙션** | 기능 | 어제 먹은 메뉴를 물어 후보에서 제외 | ✅ | v1 |
| F2 | **결과 지도** | 기능 | 제외 후 남은 가게를 지도에 표시 | ✅ | v1 |
| F3 | **스크롤 · 호버 애니메이션** | 품질 | 섹션 진입 모션, 카드 호버 반응 | ✅ | v1 |
| F4 | **모바일 최적화** | 품질 | 320px~ 전 구간. 1순위 타겟은 길 위에 있다 | ✅ | v1 |
| F5 | **배포 · SEO** | 기반 | 도메인 연결, 메타·OG, 검색 노출 | ✅ | v1 |
| F6 | **맛집 검색 · 필터** | 기능 | ~~JSON 정적 데이터 기반~~ → **카카오 로컬 API** 기반. 키워드·카테고리·좌표로 검색 | ✅ | ~~v1.1~~ **v1** (4.1) |
| F7 | **로그인 · 데이터 저장** | 기반 | Supabase Auth + DB. 이후 모든 사용자 기능의 전제 | ❌ | v2 |
| F8 | **상황 태그 리뷰 작성** | 기능 | 사진 + 데이트·혼밥·회식 등 태그 | ❌ | v2 |
| F9 | **담기 (위시리스트)** | 기능 | 가고 싶은 곳 담기. **v1은 localStorage 한정** — 내 지도 모아보기는 F7 이후 | ⚠️ 부분 | ~~v2~~ **v1 부분** (4.1) |
| F10 | **AI 리뷰 요약 · 감성 분석** | 기능 | Gemini API. 가게별 리뷰를 3줄 요약 + 긍/부정 비율 | ❌ | v3 |
| F11 | **대시보드 (도넛 · 워드클라우드)** | 기능 | 카테고리 분포, 리뷰 키워드 시각화 | ❌ | v3 |
| F12 | **취향 온보딩** | 기능 | 가입 시 몇 개 질문으로 취향 프로필 생성 | ❌ | v3 |
| F13 | **취향 기반 정렬** | 기능 | 나와 취향이 맞는 사람의 리뷰를 상단에 | ❌ | v4 |

### 5.1 의존 관계 — 만드는 순서가 정해져 있다

기능 목록은 나열 순서대로 만들 수 없다. 아래 사슬을 거스르면 **데이터가 없는 빈 기능**이 만들어진다.

```
F7 로그인·저장  →  F8 리뷰 작성  →  리뷰 축적  →  F10 AI 요약·감성분석
                                              →  F11 대시보드 차트
                                              →  F13 취향 기반 정렬
```

- **F10·F11은 리뷰 텍스트가 있어야 성립한다.** 감성 분석은 분석할 문장이, 워드클라우드는 반복되는 단어가 필요하다. v1의 씨드는 40자 메모 40개이므로 이 단계에서 돌리면 결과가 비어 보인다.
- 따라서 F10·F11의 착수 조건은 일정이 아니라 **가게당 리뷰 3건 이상, 전체 200건 이상**으로 둔다. 미달이면 v3를 열지 않는다.
- F5(배포·SEO)는 유일하게 의존 관계가 없다. **v1 첫날 처리한다.** 나중으로 미루면 OG 이미지 없이 링크가 공유되고, 그 인상은 되돌릴 수 없다.

### 5.2 F6이 되살린 것 — 태그의 소비처

v1.0 문서에서는 상황별 탐색 피드를 **유보**로 두었다. 태그를 입력만 받고 걸러낼 곳이 없다는 문제를 남겨둔 상태였다.

**F6이 이 문제를 해결한다.** JSON 정적 데이터 기반의 리스트 + 필터는 백엔드 없이 구현 가능하므로 v1.1에 넣을 수 있고, 여기서 상황 태그가 처음으로 소비된다. F8(리뷰 작성)이 태그를 입력받기 시작할 때, 그 태그가 갈 곳이 이미 준비되어 있게 된다.

> 단 v1.1 시점의 필터 대상은 씨드 40곳뿐이다. **필터를 걸수록 결과가 0건에 수렴하므로**, 필터 조합에 결과가 없을 때의 화면(인접 조건 제안)을 반드시 함께 설계한다.

---

## 6. 랜딩페이지 화면 구성 초안

**단일 페이지 세로 스크롤.** 섹션 순서가 곧 설득의 순서다.

```
┌────────────────────────────┐
│  S1  히어로 — 질문 하나      │  ← 스크롤 없이 보이는 영역
├────────────────────────────┤
│  S2  인터랙션 — 어제 뭐?     │  ← 핵심. 여기서 이탈하면 끝
├────────────────────────────┤
│  S3  결과 지도               │  ← 즉시 보상
├────────────────────────────┤
│  S4  전환 CTA               │  ← 떠나기 전에 남기게 하기
├────────────────────────────┤
│  S5  서비스 소개 · 로드맵     │  ← 궁금해진 사람만 읽음
├────────────────────────────┤
│  S6  푸터                   │
└────────────────────────────┘
```

### S1. 히어로 — **카피 확정**

```
오늘 뭐 먹지?

진짜 다녀온 사람들의 리뷰로
실패 없는 한 끼를 찾아보세요.

        [ 10초 만에 정하기 ]
```

| 요소 | 확정 내용 |
|---|---|
| 헤드 | `오늘 뭐 먹지?` — 방문자가 실제로 하고 있는 생각을 그대로 |
| 서브 | `진짜 다녀온 사람들의 리뷰로 / 실패 없는 한 끼를 찾아보세요.` (2행) |
| 버튼 | `10초 만에 정하기` — S2 인터랙션으로 진입 |

**원칙**
- 첫 화면에서 헤드·서브·버튼이 스크롤 없이 모두 보여야 한다. 브랜드 소개를 먼저 읽히려 하지 않는다.
- 서브 카피는 **2행 고정**으로 줄바꿈한다. 한 줄로 흘리면 리듬이 죽는다.
- 버튼에 위치·지역을 넣지 않는다. `내 주변 찾기` 류는 위치 권한 팝업을 기대하게 만드는데, v1은 첫 화면에서 권한을 요청하지 않는다 (7.3 참조).

> **카피가 부과하는 구현 조건** — 아래 10.5의 전제 조건이며, 지키지 못하면 이 카피를 쓸 수 없다.
> 1. 씨드 40곳은 **실제 방문 기록의 형태**로 노출한다 (작성자 표기 + 방문 날짜 + 직접 촬영 사진).
> 2. **리뷰 개수·사용자 수·누적 통계를 화면 어디에도 표시하지 않는다.** 거짓이 되는 것은 문장이 아니라 숫자다.

### S2. 질문 인터랙션 (핵심 섹션)

| 항목 | 사양 |
|---|---|
| 질문 수 | **최대 3개.** 어제 먹은 것 / 지역 / (선택) 오늘 기분 |
| 입력 방식 | **자유 입력이 아니라 칩(chip) 선택** — 7장 참조 |
| 진행 표시 | 1/3 형태의 단계 표시. 끝이 보여야 이탈하지 않는다 |
| 소요 시간 | **20초 이내** 완료 |
| 가입 | 없음. 로그인 벽을 세우지 않는다 |

### S3. 결과 지도

| 항목 | 사양 |
|---|---|
| 표시 | 지도 + 하단(모바일) 또는 좌측(데스크톱) 카드 리스트 |
| 결과 수 | **3~5개.** 많이 주는 게 아니라 좁혀주는 게 목적이다 |
| 카드 정보 | 가게명 / 카테고리 / 거리 / 한 줄 메모 |
| 제외 명시 | **"어제 드신 한식은 빼고 골랐어요"** 를 명시한다. 이 문구가 없으면 그냥 평범한 추천으로 읽힌다 |
| 결과 0건 | 빈 화면 금지. "이 동네는 아직 기록이 적어요" + 인접 지역 제안 + 첫 기록자 초대 |

> **제외했다는 사실을 말로 해주는 것이 이 서비스의 정체성이다.** 결과만 보여주면 차별점이 전달되지 않는다.

### S4. 전환 CTA

결과를 본 직후가 전환율이 가장 높은 지점이다. 이미 시간을 썼기 때문이다.

**확정 카피**

```
당신의 한 끼가 다음 사람의 실패를 막습니다.

        [ 알림 받기 ]
```

| 항목 | 내용 |
|---|---|
| 카피 | `당신의 한 끼가 다음 사람의 실패를 막습니다.` |
| 액션 | 이메일 수집 (주 CTA **1개만**) |
| 보조 문구 | "기록 기능이 열리면 가장 먼저 알려드릴게요" |

**설계 의도**
- S1이 "리뷰로 찾아드립니다"라고 약속했다면, S4는 **그 리뷰의 다음 차례가 당신이라고 말한다.** 리뷰가 적다는 사실이 약점이 아니라 참여 이유로 전환된다.
- 순서가 중요하다. 결과를 **받기 전에** 이 문장을 놓으면 요구가 되고, **받은 후에** 놓으면 초대가 된다.
- CTA는 하나만 크게. 두 개를 나란히 놓으면 둘 다 안 눌린다.
- 이메일 수집 시 수집 목적·보관 기간을 폼 하단에 명시한다.

### S5. 서비스 소개 · 로드맵

- 3줄 요약: 무엇을 / 왜 다르게 / 앞으로 무엇을
- 예정 기능 3~4개를 담백하게. **"개발 중"이라고 솔직히 적는다.** 없는 걸 있는 것처럼 쓰면 S3에서 얻은 신뢰가 깨진다.

### S6. 푸터

- 연락처(이메일 or SNS), 제작자, 문의 경로

---

## 7. 질문 인터랙션 상세 설계

### 7.1 자유 입력을 쓰지 않는 이유

"어제 뭐 드셨어요?"에 사용자는 이렇게 답한다.

```
"김치찌개"  "김찌"  "점심에 회사 앞 백반집"  "치맥"  "안 먹었는데요"
```

이걸 카테고리로 정확히 매칭하려면 자연어 처리가 필요하고, 하나라도 틀리면 **제외가 잘못 걸려 서비스의 유일한 차별점이 그 자리에서 무너진다.**

### 7.2 채택: 카테고리 칩 선택

```
어제 뭐 드셨어요?

[ 한식 ]  [ 중식 ]  [ 일식 ]  [ 양식 ]
[ 분식 ]  [ 고기 ]  [ 아시안 ]  [ 패스트푸드 ]
[ 기억 안 나요 ]
```

| 규칙 | 내용 |
|---|---|
| 카테고리 수 | 8~10개. 그 이상이면 고르는 게 일이 된다 |
| 복수 선택 | 허용 (점심·저녁 다를 수 있음). 최대 3개 |
| 회피 경로 | "기억 안 나요" 필수. 없으면 여기서 막힌다 |
| 제외 강도 | **완전 제외가 아니라 하단 배치**를 권장. 완전히 빼면 결과가 0건이 되기 쉽다 |

### 7.3 지역 입력

- **기본은 지역 선택**(드롭다운 또는 칩). 씨드 데이터가 있는 지역만 노출한다.
- **위치 권한은 선택 사항으로 둔다.** 랜딩 첫 방문에서 권한 팝업을 띄우면 이탈률이 크게 오른다. "현재 위치로 찾기" 버튼을 눌렀을 때만 요청한다.

---

## 8. 데이터 구조

v1은 **정적 파일 한 곳**에서 관리한다. 씨드 데이터 추가 시 이 파일만 수정하면 되도록 한다.

> **구현 위치 (2026-08-20):** 이 파일의 실제 경로는 `data/places.js` 가 아니라 **`assets/seed.js`** 다.
> 카카오 로컬 결과도 `server/proxy.mjs` 가 **이 스키마로 정규화**해서 내보낸다.
> 다만 카카오 결과는 `author`·`visitedAt` 이 항상 `null` 이고 `source: "kakao"` 다 — 6.3이 요구하는
> "다녀온 기록"의 조건을 채우지 못하므로 씨드 카드와 다른 컴포넌트로 렌더한다 (4.1 참조).

```js
// assets/seed.js
export const places = [
  {
    id: "place-001",
    name: "가게 이름",
    category: "korean",              // korean|chinese|japanese|western|snack|meat|asian|fastfood
    region: "seongsu",               // 씨드 대상 지역 코드
    lat: 37.5445,
    lng: 127.0557,
    note: "한 줄 메모 (최대 40자)",    // 운영자가 직접 작성
    priceRange: 2,                   // 1~4

    // --- 방문 기록으로 노출하기 위한 필드 (S1 카피의 전제, 필수) ---
    author: "JJIn",                  // 작성자 표기. 익명 데이터로 보이면 안 된다
    visitedAt: "2026-07-21",         // 실제 방문 날짜
    photo: "/img/place-001.jpg",     // 직접 촬영. 스톡 이미지 금지

    situationTags: ["date", "solo"], // v2 대비. v1에서는 표시하지 않음
    source: "seed",                  // seed | user  — 향후 사용자 리뷰와 구분

    // --- v2 이후 채워질 필드 (v1에서는 전부 비움) ---
    reviews: [],
    photos: [],
    savedCount: 0
  },
  // ... 30~50개
];
```

**규칙**
- `category` 는 7.2의 칩과 1:1로 대응한다. 값이 어긋나면 제외 로직이 작동하지 않는다.
- `source` 필드를 v1부터 넣는다. 나중에 사용자 데이터가 섞였을 때 씨드를 구분할 방법이 없으면 곤란해진다.
- `situationTags` 는 v1에서 화면에 쓰지 않지만 **입력해둔다.** 나중에 30~50곳을 다시 훑는 것보다 지금 한 번에 넣는 편이 싸다.
- `photo` 는 **직접 촬영한 사진만** 쓴다. 스톡 이미지나 가게 홍보 사진을 넣는 순간 S1 카피가 거짓이 된다.
- **리뷰 개수·사용자 수를 집계해 화면에 표시하는 필드를 만들지 않는다.** (10.5 참조)
- 값이 없는 필드는 렌더링하지 않는 조건부 구조로 만든다.

---

## 9. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 성능 | LCP 2.5초 이내. 지도는 **결과 단계에서 지연 로딩** (첫 화면에 지도 SDK를 올리지 않는다) |
| 반응형 | 320px ~ 1920px. **모바일 우선** — 1순위 타겟은 길 위에 있다 (F4) |
| 모션 | 섹션 진입 시 스크롤 애니메이션, 카드 호버 반응 (F3). **결과 지도 영역에는 모션을 쓰지 않는다** — 정보를 읽는 화면이므로 |
| 접근성 | 칩 키보드 조작, 포커스 링 유지, 지도 결과의 텍스트 대체(리스트) 제공. `prefers-reduced-motion` 존중 |
| 브라우저 | 최신 Chrome, Safari, Edge, Firefox + iOS/Android |
| SEO / 공유 | title·description·OG 이미지 (F5). **배포 첫날 처리한다** — OG 없이 공유된 링크의 인상은 되돌릴 수 없다 |
| 개인정보 | 이메일 수집 시 수집 목적·보관 기간 고지. 위치 정보는 저장하지 않고 세션 내에서만 사용 |
| 유지보수 | 씨드 1곳 추가 = 데이터 파일 1곳 수정 |

---

## 10. 리스크

### 10.1 🔴 씨드 데이터를 전국에 흩뿌리면 어디서도 비어 보인다

30~50곳은 **한 지역에 몰아야만** 밀도가 생긴다. 서울 전역에 40곳을 뿌리면 어느 동네에서 검색해도 1~2곳만 뜨고, 방문자는 "데이터 없는 사이트"로 판단한다.

**대응**: v1의 서비스 지역을 **1~2개 동네(반경 1~2km)로 한정**하고, 그 사실을 화면에 명시한다. "현재 성수·연남 지역만 지원합니다"는 미완성이 아니라 **의도된 범위**로 읽힌다. 지역을 숨기는 것이 오히려 미완성으로 보인다.

### 10.2 🟠 씨드 데이터는 운영자의 취향 편향을 그대로 담는다

40곳을 혼자 고르면 그건 개인 추천 목록이지 서비스가 아니다. 초기엔 불가피하나, **가설 3(사용자가 기록을 쌓는다)이 검증되기 전까지는 확장하지 않는다.**

### 10.3 🟠 제외 로직이 결과 0건을 만든다

지역이 좁은데 카테고리까지 빼면 남는 게 없다. → 7.2의 **"완전 제외가 아닌 하단 배치"** 로 완화한다.

### 10.5 🔴 S1 카피가 약속한 "사람들"과 실제 데이터 규모의 간극

확정 카피는 **"진짜 다녀온 사람들의 리뷰"** 를 약속한다. 그러나 v1의 리뷰는 운영자 1인이 작성한 40건이다. 방문자가 이 간극을 눈치채는 순간, 카피가 얻으려던 신뢰가 그대로 손실로 바뀐다.

**대응** — 카피를 바꾸지 않고 화면으로 해결한다.

| 지킬 것 | 이유 |
|---|---|
| 씨드를 방문 기록 형태로 노출 (작성자·방문일·실사진) | 인원이 적을 뿐, "다녀온 사람의 리뷰"라는 진술 자체는 참이 된다 |
| **리뷰 수·사용자 수·통계 미표시** | 거짓이 되는 것은 문장이 아니라 숫자다. `리뷰 40개`를 적는 순간 규모가 드러난다 |
| 스톡 이미지·홍보 사진 금지 | 사진 한 장이 "직접 다녀왔다"를 증명하거나 무너뜨린다 |
| 작성자를 익명 처리하지 않음 | 익명 다수처럼 꾸미면 과장이 되고, 실명 소수면 오히려 신뢰가 된다 |

> **검증 질문**: 방문자가 40곳을 전부 눌러본 뒤에도 속았다고 느끼지 않는가? 느낀다면 카피가 아니라 위 4개 규칙 중 무언가가 지켜지지 않은 것이다.

### 10.6 🟠 분석 기능이 데이터보다 먼저 만들어질 위험

F10(AI 요약·감성 분석)과 F11(대시보드)은 **기술적으로는 지금 당장 만들 수 있고, 실제로 만들고 싶어지는 기능**이다. 문제는 결과물이 아니라 입력이다.

| 기능 | 필요한 것 | v1 시점 현실 |
|---|---|---|
| 감성 분석 | 감정이 드러나는 문장 | 40자 메모 — 감정 표현이 거의 없음 |
| 워드클라우드 | 반복되는 단어 | 40건에서 반복 단어 5~6개 → 형태가 안 잡힘 |
| 도넛 차트 | 유의미한 분포 | 카테고리 8개에 40곳 → 조각당 5곳 |

**대응**: 5.1의 착수 조건(가게당 3건, 전체 200건)을 지킨다. 미달 상태에서 만든 대시보드는 기능이 있다는 증거가 아니라 **데이터가 없다는 증거**로 읽힌다.

> 포트폴리오 목적으로 이 기능들을 먼저 보여주고 싶다면, 서비스 화면에 붙이는 대신 **별도 데모 페이지**로 분리하고 샘플 데이터임을 명시하는 편이 안전하다. 실서비스 화면의 빈 차트는 신뢰를 깎지만, 데모라고 밝힌 화면은 그렇지 않다.

### 10.4 🟡 v1의 약속과 v2의 실물이 어긋날 수 있다

랜딩은 "찾아준다"고 말하는데, v2에 나올 제품은 "기록장"이다. 이메일을 받아둔 사람들이 출시 후 다른 물건을 만나게 된다.

**대응**: S5 소개 섹션에서 **기록이 먼저 열린다는 순서를 미리 밝힌다.** 사전 등록자에게는 "당신의 기록이 이 지도를 채웁니다"라는 맥락으로 안내한다.

---

## 11. 로드맵

| 단계 | 범위 | 핵심 기능 | 통과 조건 |
|---|---|---|---|
| **v1** | **랜딩페이지 + 맛집 담기** | F1 제외 질문, F2 결과 지도, F3 애니메이션, F4 모바일, F5 배포·SEO, 씨드 30~50곳, **F6 검색(카카오), F9 담기(localStorage)** | 질문 완료율 70% |
| ~~v1.1~~ | ~~탐색 개방 (정적)~~ | **v1에 흡수됨 (4.1)** | — |
| **v2** | 기록 제품 | F7 로그인·저장 (Supabase), F8 상황 태그 리뷰, **F9 나머지 — 담긴 목록 계정 이관 + 내 지도 모아보기** | 주당 기록 수 / 4주 잔존율 |
| v3 | 분석 · 개인화 | F10 AI 요약·감성분석 (Gemini), F11 대시보드, F12 취향 온보딩 | **리뷰 200건 · 가게당 3건** |
| v4 | 추천 | F13 취향 기반 정렬 | 취향 프로필 보유자 수 |

> **v2로 넘어가는 기준은 시간이 아니라 가설 1의 검증이다.** 질문 완료율이 목표에 크게 못 미치면 v2를 시작하기 전에 제외 로직 자체를 재설계한다.
>
> **v3의 통과 조건은 특히 엄격히 지킨다.** 리뷰가 모자란 상태의 AI 요약과 대시보드는 완성도를 높이지 않고 낮춘다 (10.6 참조).

---

## 12. 열린 이슈 (착수 전 확정 필요)

1. **서비스명** — 현재 미정. 질문형 이름("어제 뭐 드셨어요")은 정체성이 선명하나 확장성이 낮다. 가제로 시작할지 결정 필요.
2. ~~**S4 전환 CTA**~~ — ✅ **확정 (v1.1)**. 이메일 수집 1개, 카피는 "당신의 한 끼가 다음 사람의 실패를 막습니다."
3. **씨드 대상 지역** — 어느 동네 1~2곳에 40곳을 채울지. 10.1의 성패가 여기 달려 있다.
4. **카테고리 확정** — 7.2의 8~10개 칩 목록. 씨드 데이터 입력 전에 고정해야 한다.
5. ~~**지도 API**~~ — ✅ **확정 (2026-08-20). 카카오.** 검색은 카카오 로컬 REST API(`/v2/local/search/keyword`·`category`)를 `server/proxy.mjs` 경유로 쓴다. 단 **결과 지도(F2)는 여전히 외부 SDK를 쓰지 않는다** — lat/lng 바운딩 박스를 SVG viewBox에 선형 투영하는 기존 `buildMap()` 을 유지한다. 첫 화면에 지도 SDK를 올리지 않는다는 9장 LCP 요건 때문이다.
6. **기술 스택 및 배포** — 단일 페이지 규모. SEO와 향후 확장(Supabase·Gemini 연동)을 고려하면 **Next.js + Vercel**. Supabase·Gemini 모두 Next.js 라우트 핸들러와 궁합이 좋다.
7. **씨드 촬영 계획** — S1 카피가 실사진을 전제로 한다(10.5). 40곳을 실제로 방문해 사진을 확보하는 일정이 개발 일정과 별도로 필요하다. **이것이 v1의 실질적 크리티컬 패스일 가능성이 높다.**
8. **API 키 관리 · 비용** — ⚠️ **부분 해소 (2026-08-20).** 카카오 REST 키는 `server/proxy.mjs` 안에서만 읽히고 브라우저로 나가지 않는다. `.env` 는 `.gitignore` 대상이다 (저장소가 public 이므로 필수). 키가 없으면 프록시가 `503 NO_KEY` 를 주고 클라이언트는 씨드 12곳으로 폴백한다 — **초과 시 동작 = 폴백** 을 카카오에 한해 먼저 정한 셈이다. **남은 것**: 카카오 무료 티어 일일 한도 확인과 초과 시 동작, Gemini 키 정책(v3).
9. **리뷰 데이터 모델** — F7(Supabase) 도입 시 씨드 JSON을 DB로 이관해야 한다. 8장 스키마의 `source: "seed"` 필드가 이때 구분자 역할을 한다. 이관 시점과 방식은 v2에서 확정한다.

---

*이 문서는 인터뷰 기반 v1.0 초안이다. 12장의 열린 이슈가 확정되면 v1.1로 갱신한다.*
